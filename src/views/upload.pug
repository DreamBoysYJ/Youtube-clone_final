extends base

block title 
    h1 JOIN
    
    form(method="POST", enctype="multipart/form-data")
        label(for="video") Video File 
        input(type="file", accept="video/*", required, id="video", name="video")
        label(for="thumb") Thumbnail File 
        input(type="file", accept="image/*", required, id="thumb", name="thumb")
        input(placeholder="Title", required, type="text", name="title", minlength=2, 
        maxlength=80)
        input(placeholder="Description", required, type="text", name="description", minlength=2)
        input(placeholder="Hashtags", required, type="text", name="hashtags")
        input(type="submit", value="Upload Video")



